name: events
description: GDG Mien Trung Events App

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.12.9+5
  firebase_analytics: ^5.0.2
  firebase_auth: ^0.14.0+5
  firebase_core: ^0.4.0+9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
